---
name: add-a-model
description: Add or swap an open-weight model behind a routing tier in models.yaml. Use when registering a new model, replacing one that became unavailable, or when a model id returns 404 or 403. Never requires a code change.
---

Follow `docs/playbooks/add-a-model.md` exactly. It is the source of truth, shared with
every other agent and tool; this skill only points to it.

Read `AGENTS.md` first if you have not this session, and check
`docs/DECISIONS.md` before changing any behaviour it describes.
