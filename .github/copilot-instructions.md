# Copilot instructions

Read `AGENTS.md` at the repository root first — it is the single source of
project context. Then `docs/ROADMAP.md` for what is next and `docs/DECISIONS.md`
before changing any existing behaviour.

The short version: a sovereign on-premise AI workbench for SIH 2026 PS 26117.
Never claim containment you cannot demonstrate, never compute a number with a
model, and every factual claim must cite a retrieved passage.
